# Vercel SPA routing configuration
# Redirect all routes to index.html for client-side routing
/* /index.html 200

# API routes should proxy to backend
/api/* https://pronostika-platform-production.up.railway.app/api/:splat 200